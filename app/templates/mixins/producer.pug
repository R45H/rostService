//-
	data
		img - Картинка
		link - Ссылка на страницу
		title - Заголовок

mixin producer(data)
	a.producer(href= data.link ? data.link : "#")
		.producer__img(
		style="background-image: url('" + data.img + "');"
		)
		.producer__title= data.title