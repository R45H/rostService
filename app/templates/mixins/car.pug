//-
	data
		img - Картинка
		link - Ссылка на страницу
		title - Заголовок

mixin car(data)
	.car
		a.car__img(
			href= data.link ? data.link : "#"
			style="background-image: url('" + data.img + "');"
		)
		a.car__title(href= data.link ? data.link : "#")= data.title