//-
	data
		img - Картинка
		link - Ссылка на страницу
		title - Заголовок

mixin car(data)
	a.car(href= data.link ? data.link : "#")
		.car__img(style="background-image: url('" + data.img + "');")
		.car__title= data.title